# Schema for the configuration files of the hello world module.

hello_world.settings:
  type: config_object
  label: 'Content types'
  mapping:
    types:
      type: config_object
      label: 'Content types where you want to append message'
